# Contributing Guide

FitMate 프로젝트의 브랜치 전략, 커밋 컨벤션, PR 규칙을 정리한 문서다.

## 브랜치 전략

- GitHub Flow를 사용한다. main 브랜치는 항상 배포 가능한 상태로 유지한다.
- 작업은 이슈 단위로 최대한 작게 쪼갠 뒤 브랜치를 짧게 유지한다.
- 브랜치 이름은 다음 형태를 따른다.
  - 새 기능: `feature/이슈번호_설명` (예: `feature/12_login`)
  - 버그 수정: `fix/이슈번호_설명` (예: `fix/8_button-align`)
  - 테스트 작성: `test/이슈번호_설명` (예: `test/15_login`)


## 커밋 컨벤션

- 형식: `<type>: <설명>`
- 타입은 영어로, 설명은 한글로 작성한다.
- 이슈와 연결되는 작업이면 커밋 메시지 하단에 `Closes #이슈번호`를 추가한다.
- 하나의 커밋에는 하나의 논리적인 변경 사항만 담는다.

### 타입 목록

| 타입 | 의미 |
|---|---|
| feat | 새 기능 추가 |
| fix | 버그 수정 |
| docs | 문서 수정 |
| style | 코드 포맷팅, 세미콜론 등 스타일 변경 (동작 변화 없음) |
| refactor | 동작은 그대로 두고 내부 구조만 개선 |
| test | 테스트 코드 추가 및 수정 |
| chore | 빌드, 설정 등 그 외 잡다한 변경 |

### 예시

```
feat: 로그인 기능 추가

Closes #12
```

## 이슈 관리

- 버그와 기능 요청은 `.github/ISSUE_TEMPLATE`의 템플릿을 사용해서 작성한다.
- 규모가 큰 리팩토링은 이슈로 만들어서 브랜치 이름 규칙을 그대로 따른다.
- 규모가 작은 리팩토링은 이슈 없이 진행해도 된다.

## 코드 리뷰

- 개인 프로젝트라 다른 리뷰어는 없다.
- PR을 올리기 전에 AI로 잠재적인 문제를 먼저 점검한다.
- AI가 짚어준 항목은 그대로 받아들이지 않고, 왜 문제인지 직접 판단한 뒤 반영한다.

## PR 규칙

- 제목은 커밋과 같은 타입 규칙을 사용한다. (예: `feat: 로그인 기능 추가`)
- PR 본문은 `.github/PULL_REQUEST_TEMPLATE.md`를 따른다.
